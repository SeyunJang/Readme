name: Set-Common-Env-Composite
description: Set common environment variables using composite action

runs:
    using: "composite"
    steps:
        - run: echo "Setting common environment variables"
        - run: echo "##vso[task.setvariable variable=ENV1;]value1"
        - run: echo "##vso[task.setvariable variable=ENV2;]value2"
        - run: echo "##vso[task.setvariable variable=ENV3;]value3"
